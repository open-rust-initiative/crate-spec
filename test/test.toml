[package]
name = "crate-spec"
version = "0.1.0"
license = "MIT"
authors = ["shuibing", "rust"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bincode = {version = "2.0.0-rc.3", features = ["serde", "alloc"]}
openssl = {version = "0.10.53", registry = "rust"}
toml = "0.7.4"